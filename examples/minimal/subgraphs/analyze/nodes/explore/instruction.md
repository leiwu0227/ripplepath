# Explore

You are running inside the `analyze` subgraph. The parent has seeded
`state.input.topic` with the topic from kickoff. Produce at least three
distinct candidate approaches with tradeoffs. Quantity over depth — refine
will narrow it down.
