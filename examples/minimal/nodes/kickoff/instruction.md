# Kickoff

This is the first work node. Capture the topic the user wants to investigate
and a one-sentence statement of their intent.

Produce JSON that satisfies the output schema, including a tight
`handoff_summary` for downstream nodes.
