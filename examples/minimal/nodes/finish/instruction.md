# Finish

Compose the final conclusion. Read `state.outputs.analyze.findings` for the
analysis output and `state.outputs.kickoff.intent` for the user's stated
intent. Produce a one-paragraph conclusion plus the mandatory
`handoff_summary` (which will be the last summary in the transcript).
